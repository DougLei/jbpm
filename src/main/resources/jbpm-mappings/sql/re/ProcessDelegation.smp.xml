<?xml version="1.0" encoding="UTF-8"?>
<mapping-configuration>
	<sql namespace="ProcessDelegation">
		
		<!-- 删除委托明细 -->
		<content name="deleteDelegationDetails" type="select">
			delete bpm_re_delegation_detail where 
			<foreach collection="ids" alias="id" open="id in (" separator="," close=")">
				#{id, dataType=number}
			</foreach>
		</content>
		
	</sql>
</mapping-configuration>